import { FeedbackPropsOffline } from './types';
export default function InlineFeedback({ heading, description, Headers, type, count, styleConfig, footerText, onChange, onRequestClose, initialState, offlineFormData, setAnswer, footerLink, closeIconColor, }: FeedbackPropsOffline): any;

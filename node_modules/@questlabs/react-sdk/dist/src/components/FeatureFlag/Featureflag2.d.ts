import { featureFlagProps } from './featureflag.types';
export default function Featureflag2({ campaignId, userId, token, uniqueUserId, uniqueEmailId, HideMethod, variation, flagType, waitForLoading, disableCache, onTargetRetrieved, }: featureFlagProps): null;

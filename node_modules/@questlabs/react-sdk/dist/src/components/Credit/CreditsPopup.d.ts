import { default as React, FC } from 'react';
interface PopupProps {
    isOpen: boolean;
    onClose: () => void;
    headingText: React.ReactNode;
    descText: React.ReactNode;
    isCloseble: boolean;
    continueButton: boolean;
    buttonText: string;
    buttonFunction: () => void;
}
declare const CreditsPopup: FC<PopupProps>;
export default CreditsPopup;

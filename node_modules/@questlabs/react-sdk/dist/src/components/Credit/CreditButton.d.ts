import { default as React, FC } from 'react';
interface CreditButtonProps {
    remainingCount: React.ReactNode;
    addButton: boolean;
    fontSize: string;
    buttonFunction: () => void;
    btnText: string;
}
declare const CreditButton: FC<CreditButtonProps>;
export default CreditButton;

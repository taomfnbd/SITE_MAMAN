import { default as React } from 'react';
interface PropType {
    heading?: string;
    description?: string;
    iconColor?: string;
    onClose?: () => void;
    style?: React.CSSProperties & {
        headingStyle?: React.CSSProperties;
        descriptionStyle?: React.CSSProperties;
        iconStyle?: React.CSSProperties;
        topbarStyle?: React.CSSProperties;
    };
}
export default function TopBar({ heading, description, iconColor, onClose, style, }: PropType): import("react/jsx-runtime").JSX.Element;
export {};

import { SearchProps } from './searchTypes';
export default function SearchWrapper({ wholerScreen, defaultResult, defulatResultLength, onSearch, questId, token, userId, onResultClick, uniqueUserId, uniqueEmailId, styleConfig, showFooter, iconColor, footerText, footerLink, variation, placeholder, open, icons, sections, buttonText, searchDetails, }: SearchProps): JSX.Element;

import { FeedbackPropsOffline } from './types';
export default function InlineFeedbackOffline({ heading, description, Headers, type, count, styleConfig, footerText, onChange, onRequestClose, initialState, offlineFormData, setAnswer, footerLink, BrandTheme, QuestThemeData, showFooter, }: FeedbackPropsOffline): any;

import { CSSProperties } from 'react';
interface propType {
    options: string[] | [] | any;
    checked?: string[];
    selectedStyle?: ExtendedCSSProperties;
    style?: ExtendedCSSProperties;
    onChange?: (e: {
        target: {
            value: string;
            checked: boolean;
        };
    }) => void;
}
type ExtendedCSSProperties = CSSProperties & {
    borderColor?: string;
    accentColor?: string;
    color?: string;
};
interface propTypeTwo {
    selectedStyle?: ExtendedCSSProperties;
    options: string[] | [] | any;
    checked?: string[];
    style?: ExtendedCSSProperties;
    onChange?: (e: {
        target: {
            value: string;
            checked: boolean;
        };
    }) => void;
}
export declare const MultiChoice: ({ options, checked, selectedStyle, style, onChange, }: propType) => import("react/jsx-runtime").JSX.Element;
export declare const MultiChoiceTwo: ({ options, checked, style, selectedStyle, onChange, }: propTypeTwo) => import("react/jsx-runtime").JSX.Element;
export {};

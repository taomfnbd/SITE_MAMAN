import { default as React, ReactNode } from 'react';
import { GamifiedQuizHeader } from './Header';
import { GamifiedQuizContent } from './GamifiedQuizContent';
import { GamifiedQuizFooter } from './Footer';
import { GamifiedQuizProps } from '../types/GamifiedQuiz.type';
interface GamifiedQuizComponents {
    Header: typeof GamifiedQuizHeader;
    Content: typeof GamifiedQuizContent;
    Footer: typeof GamifiedQuizFooter;
}
interface GamifiedQuizExtended extends GamifiedQuizProps {
    children?: ReactNode;
}
declare const GamifiedQuiz: React.FC<GamifiedQuizExtended> & GamifiedQuizComponents;
export default GamifiedQuiz;

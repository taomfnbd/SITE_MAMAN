"use strict";
// import axios from "axios";
// import * as variables from "../../utils/config";
// import { common, instagramTypes } from "../../types";
// class Instagram {
//   private apiKey: string;
//   private apiSecret: string;
//   private config: common.config;
//   private baseUrl: string;
//   constructor(config: common.constructorConfig) {
//     this.apiKey = config.apiKey;
//     this.apiSecret = config.apiSecret;
//     this.baseUrl =
//       config?.testMode == true ? variables.serverTestUrl : variables.serverUrl;
//     this.config = {
//       headers: { apiKey: this.apiKey, apiSecret: this.apiSecret, "User-Agent": config.userAgent, },
//     };
//   }
//   async connect({
//     entityId,
//     userId,
//     token,
//     instagramRedirectCode,
//   }: instagramTypes.connectInstagram) {
//     try {
//       if (!instagramRedirectCode || instagramRedirectCode == "") {
//         throw new Error("Invalid instagramRedirectCode");
//       }
//       if (!entityId || entityId == "") {
//         throw new Error("Invalid entityId");
//       }
//       if (!userId || userId == "") {
//         throw new Error("Invalid userId");
//       }
//       if (!token || token == "") {
//         throw new Error("Invalid token");
//       }
//       const headers = { ...this.config.headers, userId, token };
//       const body = {
//         code: instagramRedirectCode,
//         entityId,
//         userId,
//       };
//       const response = await axios.post(
//         `${this.baseUrl}/instagram/login`,
//         body,
//         { headers }
//       );
//       return response.data;
//     } catch (error) {
//       throw new Error(error.message);
//     }
//   }
//   async disconnect({
//     entityId,
//     userId,
//     token,
//   }: instagramTypes.disconnectInstagram) {
//     try {
//       if (!entityId || entityId == "") {
//         throw new Error("Invalid entityId");
//       }
//       if (!userId || userId == "") {
//         throw new Error("Invalid userId");
//       }
//       if (!token || token == "") {
//         throw new Error("Invalid token");
//       }
//       const headers = { ...this.config.headers, userId, token };
//       const body = {
//         entityId,
//         userId,
//       };
//       const response = await axios.post(
//         `${this.baseUrl}/instagram/logout`,
//         body,
//         { headers }
//       );
//       return response.data;
//     } catch (error) {
//       throw new Error(error.message);
//     }
//   }
// }
// export default Instagram;

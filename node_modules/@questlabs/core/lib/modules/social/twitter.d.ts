import QuestSdk from "../..";
import { IPostRequestOptions } from "../../types/common.types";
import { IConnectTwitter, IConnectTwitterResponse } from "../../types/twitter.types";
export declare class Twitter {
    questSdk: QuestSdk;
    constructor(questSdk: QuestSdk);
    connect({ redirectUri, twitterRedirectCode, entityId, type }: IConnectTwitter, options?: IPostRequestOptions): Promise<IConnectTwitterResponse>;
    disconnect(entityId: string, userId: string, token: string): Promise<void>;
}
export default Twitter;

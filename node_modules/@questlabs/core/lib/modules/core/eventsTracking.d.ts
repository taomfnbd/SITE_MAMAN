import QuestSdk from "../..";
import { IPostRequestOptions } from "../../types/common.types";
import { ITrack, ITrackResponse } from "../../types/eventsTracking.types";
export declare class QuestEvents {
    questSdk: QuestSdk;
    constructor(questSdk: QuestSdk);
    track({ entityId, name, userId, properties, isIndependent, isQuestUser, queue }: ITrack, options?: IPostRequestOptions): Promise<ITrackResponse>;
}

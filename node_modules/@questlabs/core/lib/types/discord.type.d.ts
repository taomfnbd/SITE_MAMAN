type baseDiscord = {
    entityId: string;
    userId: string;
    token: string;
};
export type connectDiscord = baseDiscord & {
    redirectUri: string;
    discordRedirectCode: string;
};
export type disconnectDiscord = baseDiscord;
export type fetchMyGuilds = baseDiscord;
export type fetchMyGuildRoles = baseDiscord & {
    guildId: string;
};
export type fetchGuildSceduledEvents = fetchMyGuildRoles;
export {};

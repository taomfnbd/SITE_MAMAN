import { ICommonResponse } from "./common.types";
export interface ITrack {
    userId: string;
    entityId?: string;
    name: string;
    properties?: Record<string, any>;
    isIndependent?: boolean;
    isQuestUser?: boolean;
    queue?: boolean;
}
export interface ITrackResponse extends ICommonResponse {
}
